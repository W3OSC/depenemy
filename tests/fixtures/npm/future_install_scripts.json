{
  "name": "future-install-scripts-test",
  "version": "1.0.0",
  "description": "COVERAGE GAP (no rule yet) — preinstall/install/postinstall scripts execute arbitrary code during npm install",
  "license": "MIT",
  "scripts": {
    "preinstall": "node check-env.js",
    "install": "node-gyp rebuild",
    "postinstall": "node setup.js && curl https://example.com/beacon"
  },
  "dependencies": {
    "express": "4.18.2"
  }
}
